tion# Environment Variables Template for Project B Integration
# Copy this to supabase/.env.local and fill in your actual values

# Project B Connection (for Edge Functions)
# Get these from Project B Dashboard → Settings → API
PROJECT_B_URL=https://your-project-b.supabase.co
PROJECT_B_SERVICE_ROLE_KEY=your-project-b-service-role-key-here

# SECURITY NOTES:
# - NEVER commit .env.local to git (already in .gitignore)
# - Service role key has full database access - keep it secret
# - For production, set these in Supabase Dashboard → Edge Functions → Secrets
